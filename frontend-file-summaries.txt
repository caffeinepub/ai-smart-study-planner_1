{
  "frontend/src/index.css": "Global styles with OKLCH indigo palette, Plus Jakarta Sans font, page transition animations, and a new reusable `.glass-card` component class in `@layer components` providing semi-transparent backgrounds, `backdrop-filter: blur(12px)`, and soft low-opacity borders for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with indigo-primary design tokens, Plus Jakarta Sans font, custom indigo-tinted card/tab/primary shadows, additional border-radius tokens, and slide-up/scale-in animations.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions with full guest mode support — `useGetWeeklyProgress` and `useGetStudyStreak` now compute progress locally from localStorage for guest users instead of calling the backend, and both hooks return safe default values (empty entries, 0n) when no exam exists rather than throwing errors.",
  "frontend/src/hooks/useTheme.ts": "Custom hook managing light/dark theme state with localStorage persistence and document root class toggling.",
  "frontend/src/App.tsx": "Main app with TanStack Router that redirects `/` directly to `/dashboard` with no auth check; removes the `ProtectedLayout` auth guard and replaces it with an open `AppLayout`; `ProfileSetupModal` is only shown for authenticated non-guest users; no redirect-to-login effects remain.",
  "frontend/src/components/Layout.tsx": "App shell with sticky indigo-themed header (app icon + title + theme toggle), scrollable main content area, and fixed BottomTabBar.",
  "frontend/src/components/BottomTabBar.tsx": "Redesigned bottom navigation with elevated card-style, rounded-t-2xl, indigo active tab indicators with dot marker, and press scale animations on all five tabs.",
  "frontend/src/components/ThemeToggle.tsx": "Icon button toggling between sun/moon icons to switch light/dark mode via the useTheme hook.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-login modal for authenticated users to enter their name, now skipped entirely for guest users via useGuestMode check.",
  "frontend/src/components/PremiumGate.tsx": "Wrapper component using `useConsolidatedPremiumStatus`; renders children (not the upgrade prompt) while premium status is still loading to prevent false paywall flashes for premium users.",
  "frontend/src/components/UpgradePrompt.tsx": "Redesigned upgrade prompt with indigo gradient icon, feature list with highlighted gated feature, and gradient CTA button with arrow icon.",
  "frontend/src/components/ReminderBanners.tsx": "Dismissible alert banners for upcoming exam warnings and pending daily tasks — updated all `flex-shrink-0` classes to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/pages/LoginPage.tsx": "Redesigned login page with animated entrance, indigo gradient background blobs, feature pills, gradient primary CTA button with press animation, and guest mode secondary button.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page using correct hook APIs (`displayName` from `useLocalProfile`, `currentStreak` from `useLocalStreak`, `Exam | null` for `useExamCountdown`, `bigint` IDs for task mutations) — shows a Revision Day banner and hides motivational/study-tip cards when all today's tasks are revision tasks.",
  "frontend/src/pages/ExamSetup.tsx": "Exam setup form that now uses `useConsolidatedPremiumStatus` instead of ad-hoc premium checks; defers the locked state until premium status is confirmed (shows a loading spinner while checking), preventing premium users from seeing the paywall during actor initialization.",
  "frontend/src/pages/FocusMode.tsx": "Pomodoro focus mode page with the phase toggle, reset button, session counter, and advanced settings panel all updated to use the shared `.glass-card` utility class for a consistent frosted-glass appearance.",
  "frontend/src/pages/ProgressTracking.tsx": "Progress analytics page showing streak, completion stats, and weekly bar chart with skeleton loading and empty states — removed the error banner that previously appeared when backend progress data failed to load, silently falling back to local data instead.",
  "frontend/src/pages/Settings.tsx": "Fully rewritten Settings page with no cloud sync or authentication references — includes a Profile section with inline display name editing (stored in localStorage), an Appearance section, a Theme Customization section using ThemeSelectorSection, premium upgrade banner, subscription status, developer options, and about.",
  "frontend/src/hooks/usePremiumTestingMode.ts": "Custom hook that manages a premium testing mode flag stored in localStorage under 'premiumTestingMode', providing isPremiumTestingEnabled state and a togglePremiumTesting function.",
  "frontend/src/hooks/useGuestMode.ts": "Custom hook managing guest session state via localStorage — now auto-initializes guest mode as `true` on first load (if no prior value is stored), ensuring a device ID is created and persisted without any user interaction.",
  "frontend/src/hooks/useSubscription.ts": "Subscription abstraction hook with a mock SubscriptionProvider interface supporting free trial, monthly, and yearly plans stored in localStorage, designed for future real SDK integration.",
  "frontend/src/contexts/SubscriptionContext.tsx": "React context for subscription state, wrapping useSubscription hook — fixed the name conflict by splitting the import so SubscriptionProvider and SubscriptionStatus are imported as type-only, while useSubscription is imported as a value.",
  "frontend/src/hooks/usePaywallTrigger.ts": "Hook that determines when to show the paywall based on task completion and dismissal state stored in localStorage, only triggering after the first task is completed.",
  "frontend/src/types/features.ts": "TypeScript type definitions for the premium feature taxonomy, including PremiumFeature union type, FeatureConfig interface, and FEATURE_CONFIG constant with display names, descriptions, and icons for all five premium features.",
  "frontend/src/hooks/useCloudSync.ts": "Hook encapsulating cloud sync logic that checks guest mode status and either shows a login prompt (for guests) or performs the sync operation (for authenticated users).",
  "frontend/src/hooks/useGuestToAuthMigration.ts": "Hook handling guest-to-authenticated migration by clearing guest mode flags when a user logs in with Internet Identity.",
  "frontend/src/components/CloudSyncPrompt.tsx": "Dialog component shown to guest users attempting cloud sync, explaining that Internet Identity login is required and providing Login Now / Not Now actions.",
  "frontend/src/components/PaywallScreen.tsx": "Redesigned paywall modal with indigo gradient header, check-icon feature list, highlighted \"Best Value\" yearly plan badge, and gradient trial CTA button with press animation.",
  "frontend/src/main.tsx": "Application entry point wrapping the app with QueryClientProvider, InternetIdentityProvider, and SubscriptionProvider for global subscription state access.",
  "frontend/src/hooks/useLocalStreak.ts": "Custom hook managing daily study streak in localStorage with robust error handling for JSON parse failures, invalid dates, and storage errors — always returns safe default values (0) and never throws unhandled exceptions.",
  "frontend/src/hooks/useLocalDailyProgress.ts": "Custom hook that calculates and persists daily progress percentage in localStorage with comprehensive error handling, automatic day-reset logic, and safe default values (0) when data is unavailable or corrupted.",
  "frontend/src/hooks/useExamCountdown.ts": "Custom hook that computes days remaining until an exam date from a bigint nanosecond timestamp, returning daysRemaining, isToday, and isPast flags — works fully offline.",
  "frontend/src/hooks/useCelebrationTrigger.ts": "Custom hook that triggers a celebration animation exactly once per day when progress reaches 100%, using localStorage to prevent re-triggering on the same day.",
  "frontend/src/components/CelebrationAnimation.tsx": "Non-blocking canvas-based confetti celebration overlay that auto-dismisses after ~2 seconds, rendered with pointer-events-none so the user can still interact with the page beneath.",
  "frontend/src/components/QuickActions.tsx": "Quick action pill buttons row now using the shared `.glass-card` utility class on each button, with semantic icon color tokens replacing the previous white-on-dark color scheme for proper light/dark mode support.",
  "frontend/src/components/DailyMotivationCard.tsx": "Daily motivational quote card now using the shared `.glass-card` utility class with a subtle indigo/violet tinted overlay, replacing the previous solid gradient background while maintaining strong text contrast via semantic foreground tokens.",
  "frontend/src/components/StudyTipCard.tsx": "Daily study tip card now using the shared `.glass-card` utility class with a subtle emerald/cyan tinted overlay, replacing the previous solid gradient background while keeping the lightbulb icon and tip text fully readable.",
  "frontend/src/components/NextFocusSessionCard.tsx": "Next focus session suggestion card now using the shared `.glass-card` utility class with a subtle indigo tinted overlay; updated icon and button colors to use semantic design tokens for consistent light/dark mode contrast.",
  "frontend/src/utils/premiumCheck.ts": "New utility hook `useConsolidatedPremiumStatus` that checks all three premium sources (backend profile UserTier, SubscriptionContext isPremium, and usePremiumTestingMode) and returns both `isPremium` and `isLoading` to prevent false paywall flashes during profile fetch.",
  "frontend/src/components/AdvancedFocusSettings.tsx": "Advanced focus settings panel now uses `useConsolidatedPremiumStatus` for consistent premium checking; shows a brief loading state instead of the paywall while subscription status is being determined.",
  "frontend/src/components/AdvancedStatisticsSection.tsx": "Advanced analytics section now uses `useConsolidatedPremiumStatus` for consistent premium checking; shows a brief loading state instead of the paywall while subscription status is being determined.",
  "frontend/src/components/SmartStudyInsightsCard.tsx": "Smart study insights card now uses `useConsolidatedPremiumStatus` for consistent premium checking; shows a brief loading state instead of the paywall while subscription status is being determined.",
  "frontend/src/components/ThemeSelectorSection.tsx": "Theme preset selector grid with active-state highlight (border + checkmark), lock icons for premium-only themes, and FREE badges for free themes — calls the fixed `useCustomTheme.selectTheme` on click.",
  "frontend/src/hooks/useLocalProfile.ts": "New hook that reads and writes a user display name to localStorage under the key 'user_display_name', returning the current name and a setter function — no authentication required.",
  "frontend/src/hooks/useCustomTheme.ts": "Custom hook managing theme preset selection — fixes CSS variable names to match what Tailwind/index.css actually uses (`--primary`, `--accent`, `--ring` with raw OKLCH values), persists selection to localStorage, and applies theme on mount and on change."
}