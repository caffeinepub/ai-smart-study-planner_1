{
  "frontend/src/index.css": "Global styles with OKLCH indigo palette, Plus Jakarta Sans font, page transition animations, reusable `.glass-card` component class, and a subtle professional background image applied via `body::before` pseudo-element at 18% opacity in light mode and 10% in dark mode — keeping all UI elements fully legible while adding visual depth.",
  "frontend/tailwind.config.js": "Extended Tailwind config adding a `sway` keyframe animation (gentle ±2° rotation over 3.5s ease-in-out infinite) and its corresponding `animate-sway` utility class, alongside all existing design tokens.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions with full guest mode support, plus new useBackupData, useRestoreData, and useGetLatestBackup mutations/queries that call the backend backup/restore endpoints with success/error toast notifications.",
  "frontend/src/hooks/useTheme.ts": "Custom hook managing light/dark theme state with localStorage persistence and document root class toggling.",
  "frontend/src/App.tsx": "Main app with TanStack Router that redirects `/` directly to `/dashboard` with no auth check; removes the `ProtectedLayout` auth guard and replaces it with an open `AppLayout`; `ProfileSetupModal` is only shown for authenticated non-guest users; no redirect-to-login effects remain.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header now using the uploaded custom Studiora logo image (open book with green sprout) instead of the previous app-icon-indigo asset, with `object-contain` to preserve the logo's transparent background.",
  "frontend/src/components/BottomTabBar.tsx": "Redesigned bottom navigation with elevated card-style, rounded-t-2xl, indigo active tab indicators with dot marker, and press scale animations on all five tabs.",
  "frontend/src/components/ThemeToggle.tsx": "Icon button toggling between sun/moon icons to switch light/dark mode via the useTheme hook.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-login profile setup modal with the welcome title updated from \"Welcome to StudyPlan!\" to \"Welcome to Studiora!\" for consistent branding.",
  "frontend/src/components/PremiumGate.tsx": "Premium gate wrapper that shows a soft non-blocking upgrade prompt (\"Available in Premium to enhance your study experience.\") with optional blurred content preview and a PaywallScreen modal trigger for non-premium users.",
  "frontend/src/components/UpgradePrompt.tsx": "Soft upgrade prompt component with compact and full variants showing premium feature highlights and a \"Learn More & Upgrade\" CTA that opens the PaywallScreen modal.",
  "frontend/src/components/ReminderBanners.tsx": "Dismissible alert banners for upcoming exam warnings and pending daily tasks — updated all `flex-shrink-0` classes to `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/pages/LoginPage.tsx": "Login/splash screen now displaying the uploaded custom Studiora logo image prominently in the hero section, replacing the previous app-icon-indigo asset, with `object-contain` and `drop-shadow-lg` for clean rendering.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page with hero header, stats, quick actions, today's tasks, reminder banners, AI insights summary card, study companion, motivation/tip cards, and next focus session — now correctly passes `exams` prop to ReminderBanners and `activeExam`/`tasks` props to NextFocusSessionCard.",
  "frontend/src/pages/ExamSetup.tsx": "Exam setup form using `useSubmitExamSetup` (fixed from non-existent `useCreateExam`) and `isGuestMode` (fixed from `isGuest`); all users can create study plans without premium restriction.",
  "frontend/src/pages/FocusMode.tsx": "Pomodoro focus mode page with basic timer fully accessible to all users; advanced settings (AdvancedFocusSettings) are premium-gated and ambient sounds are restricted to premium users via gatedSound.",
  "frontend/src/pages/ProgressTracking.tsx": "Progress tracking page with basic stats always visible to free users; fixed `useLocalStreak` and `useLocalDailyProgress` usage to correctly destructure `currentStreak` and `percentage` from their returned objects.",
  "frontend/src/pages/Settings.tsx": "Settings page with profile, appearance, subscription, and backup sections; fixed to use `currentPlan` and `isPremium` from the new SubscriptionContext (removed `subscription` object reference) and added `isOpen` prop to PaywallScreen.",
  "frontend/src/hooks/usePremiumTestingMode.ts": "Custom hook managing premium testing mode flag in localStorage — reads the value explicitly (only `'true'` string returns `true`), persists changes via `useEffect`, and exposes `togglePremiumTesting` that accepts an optional boolean argument (from Radix Switch's `onCheckedChange`) or toggles when called without arguments.",
  "frontend/src/hooks/useGuestMode.ts": "Custom hook managing guest session state via localStorage — now auto-initializes guest mode as `true` on first load (if no prior value is stored), ensuring a device ID is created and persisted without any user interaction.",
  "frontend/src/hooks/useSubscription.ts": "Refactored subscription hook supporting Free/Premium tiers with product IDs 'studiora_monthly' ($3.99/month) and 'studiora_yearly' ($29.99/year), localStorage persistence, auto-restore on mount, and Google Play Billing integration via launchBillingFlow/acknowledgePurchase.",
  "frontend/src/contexts/SubscriptionContext.tsx": "React context wrapping useSubscription that exposes isPremium, currentPlan, subscribeToPlan, restorePurchases, and cancelSubscription; auto-calls queryPurchases on mount to restore premium state from localStorage.",
  "frontend/src/hooks/usePaywallTrigger.ts": "Hook that determines when to show the paywall based on task completion and dismissal state stored in localStorage, only triggering after the first task is completed.",
  "frontend/src/types/features.ts": "Premium feature type definitions with ad_free_experience removed — updated PremiumFeature union type, FEATURE_CONFIG record, and PREMIUM_FEATURES array to contain only the 6 remaining productivity/personalization features.",
  "frontend/src/hooks/useCloudSync.ts": "Hook encapsulating cloud sync logic that checks guest mode status and either returns a prompt flag (for guests) or a synced flag (for authenticated users).",
  "frontend/src/hooks/useGuestToAuthMigration.ts": "Hook handling guest-to-authenticated migration by clearing guest mode flags when a user logs in with Internet Identity.",
  "frontend/src/components/CloudSyncPrompt.tsx": "Dialog component shown to guest users attempting cloud sync, explaining that Internet Identity login is required and providing Login Now / Not Now actions.",
  "frontend/src/components/PaywallScreen.tsx": "Redesigned paywall modal showing \"Upgrade to Studiora Premium\" title, 7 premium feature benefits with icons, monthly ($3.99) and yearly ($29.99 Best Value) plan cards, \"Continue with Google Play\" CTA, Restore Purchases button, and inline error/success handling.",
  "frontend/src/main.tsx": "Application entry point with QueryClientProvider, InternetIdentityProvider, SubscriptionProviderComponent, and Toaster — uses the renamed SubscriptionProviderComponent export.",
  "frontend/src/hooks/useLocalStreak.ts": "Custom hook managing daily study streak in localStorage with robust error handling for JSON parse failures, invalid dates, and storage errors — always returns safe default values (0) and never throws unhandled exceptions.",
  "frontend/src/hooks/useLocalDailyProgress.ts": "Custom hook that calculates and persists daily progress percentage in localStorage with comprehensive error handling, automatic day-reset logic, and safe default values (0) when data is unavailable or corrupted.",
  "frontend/src/hooks/useExamCountdown.ts": "Custom hook that computes days remaining until an exam date from a bigint nanosecond timestamp, returning daysRemaining, isToday, and isPast flags — works fully offline.",
  "frontend/src/hooks/useCelebrationTrigger.ts": "Custom hook that triggers a celebration animation exactly once per day when progress reaches 100%, using localStorage to prevent re-triggering on the same day.",
  "frontend/src/components/CelebrationAnimation.tsx": "Non-blocking canvas-based confetti celebration overlay that auto-dismisses after ~2 seconds, rendered with pointer-events-none so the user can still interact with the page beneath.",
  "frontend/src/components/QuickActions.tsx": "Quick action pill buttons row now using the shared `.glass-card` utility class on each button, with semantic icon color tokens replacing the previous white-on-dark color scheme for proper light/dark mode support.",
  "frontend/src/components/DailyMotivationCard.tsx": "Daily motivational quote card now using the shared `.glass-card` utility class with a subtle indigo/violet tinted overlay, replacing the previous solid gradient background while maintaining strong text contrast via semantic foreground tokens.",
  "frontend/src/components/StudyTipCard.tsx": "Daily study tip card now using the shared `.glass-card` utility class with a subtle emerald/cyan tinted overlay, replacing the previous solid gradient background while keeping the lightbulb icon and tip text fully readable.",
  "frontend/src/components/NextFocusSessionCard.tsx": "Next focus session suggestion card now using the shared `.glass-card` utility class with a subtle indigo tinted overlay; updated icon and button colors to use semantic design tokens for consistent light/dark mode contrast.",
  "frontend/src/utils/premiumCheck.ts": "Consolidated premium status hook that derives isPremium from SubscriptionContext (Google Play Billing state) with legacy compatibility fields for trial status.",
  "frontend/src/components/AdvancedFocusSettings.tsx": "Advanced focus settings panel (custom intervals, ambient sounds, session history) wrapped in PremiumGate with blurred preview; basic Pomodoro timer remains fully accessible to free users.",
  "frontend/src/components/AdvancedStatisticsSection.tsx": "Advanced analytics section wrapped in PremiumGate with blurred preview; shows detailed weekly bar chart and completion rate stats for premium users, loading skeleton while status resolves.",
  "frontend/src/components/SmartStudyInsightsCard.tsx": "Smart study insights card showing AI-powered or static insights for premium users, with a blurred teaser and upgrade CTA for free users; fixed PaywallScreen usage to use `isOpen` prop instead of removed `featureName` prop.",
  "frontend/src/components/ThemeSelectorSection.tsx": "Theme preset selector grid using `activeThemeId` (fixed from `selectedTheme`) from `useCustomTheme`; free users get 3 themes, premium themes show lock icons and open PaywallScreen when tapped.",
  "frontend/src/hooks/useLocalProfile.ts": "New hook that reads and writes a user display name to localStorage under the key 'user_display_name', returning the current name and a setter function — no authentication required.",
  "frontend/src/hooks/useCustomTheme.ts": "Custom hook managing theme preset selection — fixes CSS variable names to match what Tailwind/index.css actually uses (`--primary`, `--accent`, `--ring` with raw OKLCH values), persists selection to localStorage, and applies theme on mount and on change.",
  "frontend/src/hooks/useStudyCompanion.ts": "Custom hook that computes a plant growth stage (0–5) from local streak days and completed focus work sessions, persists the companion state to localStorage, and reactively recomputes whenever streak or session history changes.",
  "frontend/src/components/StudyCompanion.tsx": "Visual study companion card displaying one of six distinct inline SVG plant illustrations (seed → sprout → seedling → young plant → blooming → fully grown) with a progress bar, stage badge, and calming encouraging message — all driven by the useStudyCompanion hook with a gentle sway animation.",
  "frontend/src/hooks/useShareStudyPlan.ts": "Custom hook that reads the active exam from useGetAllExams and useExamCountdown, formats it into a shareable text string (exam name, date, countdown, daily hours, subjects/topics), and exposes a shareStudyPlan() function that uses the Web Share API with a clipboard fallback.",
  "frontend/src/components/ShareButton.tsx": "Reusable share button component using the useShareStudyPlan hook; shows a Share2 icon and cycles through idle/sharing/shared/copied/error states with appropriate icons and colors, rendering null when no active exam exists.",
  "frontend/src/hooks/useAmbientSound.ts": "Web Audio API hook for ambient sound playback; fixed `'white_noise'` comparison to `'whitenoise'` to match the updated `AmbientSound` type in `AdvancedFocusSettings`.",
  "frontend/src/hooks/useAIInsights.ts": "New custom hook that analyzes local study data (streak, daily progress, focus sessions, exam tasks) and generates a memoized, priority-sorted array of personalized AIInsight objects covering 6+ insight rules including streak consistency, completion rate trends, subject weak spots, revision timing, session frequency, and overall motivational feedback — no external API calls.",
  "frontend/src/components/AIInsightsPanel.tsx": "AI insights panel wrapped in PremiumGate with blurred preview; renders type-specific insight cards (suggestion/encouragement/warning/feedback) from useAIInsights with expand/collapse for premium users.",
  "frontend/src/components/AIInsightsSummaryCard.tsx": "Compact dashboard preview card showing the top 1–2 highest-priority AI insights (title + icon only) with a \"View All Insights\" link to /progress, premium badge for non-premium users, and appropriate loading/empty states.",
  "frontend/index.html": "HTML entry point with updated document title reading \"Studiora — Study Planner\" for browser tab and store branding.",
  "frontend/src/components/AdBanner.tsx": "AdBanner component that returns null to completely hide ads across the app since ads are no longer part of the product offering.",
  "frontend/src/utils/backup.ts": "New utility module that collects all user data from localStorage (exams, streak, companion state, theme, subscription, focus sessions) into a `StudiorBackupPayload` JSON object, then exports it via the Web Share API (with a programmatic download fallback) as `studiora_backup.json`; also exports `restoreFromFile` which opens a file picker, validates the JSON, and writes all fields back to localStorage.",
  "frontend/src/hooks/useBackupRestore.ts": "Custom React hook wrapping `createBackup` and `restoreFromFile` from the backup utility; manages `lastBackup` timestamp (persisted to localStorage), loading states, and auto-clearing success/error messages; exposes `handleBackup` and `handleRestore` (which accepts a promise-based confirmation callback) plus `clearMessages`.",
  "frontend/src/utils/googlePlayBilling.ts": "Simulated Google Play Billing integration layer with Promise-based API (initBillingClient, launchBillingFlow, acknowledgePurchase, queryPurchases) that persists purchases to localStorage under 'studiora_purchases' and mirrors the Google Play Billing Library interface."
}